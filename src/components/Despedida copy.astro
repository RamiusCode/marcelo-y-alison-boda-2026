---
// Despedida.astro
---
<section class="py-4 px-4 relative overflow-hidden">
  <div class="max-w-6xl mx-auto relative z-10">
    <div class="max-w-4xl mx-auto text-center">

      <div class="flex justify-center mb-10" id="flores-arriba-despedida">
        <img
          src="/imagenes/Separadores.webp"
          alt="Flores decorativas"
          class="w-40 md:w-48 lg:w-56 h-auto object-contain opacity-90"
        />
      </div>

      <div 
        class="relative backdrop-blur-sm bg-white/10 rounded-[2.5rem] border-2 border-[#C9A961]/20 p-10 md:p-12 lg:p-16" 
        id="card-despedida"
      >
        <div class="absolute top-0 left-0 right-0 h-1/2 bg-gradient-to-b from-white/10 to-transparent rounded-t-[2.5rem] pointer-events-none"></div>

        <div class="absolute top-4 left-4 w-16 h-16">
          <div class="absolute top-0 left-0 w-full h-0.5 bg-gradient-to-r from-[#C9A961]/40 to-transparent"></div>
          <div class="absolute top-0 left-0 w-0.5 h-full bg-gradient-to-b from-[#C9A961]/40 to-transparent"></div>
        </div>
        <div class="absolute top-4 right-4 w-16 h-16">
          <div class="absolute top-0 right-0 w-full h-0.5 bg-gradient-to-l from-[#C9A961]/40 to-transparent"></div>
          <div class="absolute top-0 right-0 w-0.5 h-full bg-gradient-to-b from-[#C9A961]/40 to-transparent"></div>
        </div>
        <div class="absolute bottom-4 left-4 w-16 h-16">
          <div class="absolute bottom-0 left-0 w-full h-0.5 bg-gradient-to-r from-[#C9A961]/40 to-transparent"></div>
          <div class="absolute bottom-0 left-0 w-0.5 h-full bg-gradient-to-t from-[#C9A961]/40 to-transparent"></div>
        </div>
        <div class="absolute bottom-4 right-4 w-16 h-16">
          <div class="absolute bottom-0 right-0 w-full h-0.5 bg-gradient-to-l from-[#C9A961]/40 to-transparent"></div>
          <div class="absolute bottom-0 right-0 w-0.5 h-full bg-gradient-to-t from-[#C9A961]/40 to-transparent"></div>
        </div>

        <div class="relative z-10">
          <div id="titulo-despedida" class="mb-8">
            <h2 class="font-charm text-3xl md:text-3xl lg:text-3xl text-[#C9A961] font-bold mb-4">
              Querida Familia y Amigos
            </h2>
            <div class="flex items-center justify-center gap-3">
              <div class="w-16 h-[2px] bg-gradient-to-r from-transparent to-[#C9A961]"></div>
              <div class="w-2 h-2 rounded-full bg-[#C9A961]"></div>
              <div class="w-16 h-[2px] bg-gradient-to-l from-transparent to-[#C9A961]"></div>
            </div>
          </div>

          <div id="mensaje-despedida" class="space-y-6 mb-10">
            <p class="parrafo-animado font-charm text-xl md:text-2xl lg:text-3xl text-[#626a54] leading-relaxed">
              Nos llena de felicidad poder compartir con ustedes este momento tan único y especial.
            </p>
            <p class="parrafo-animado font-charm text-xl md:text-2xl lg:text-3xl text-[#626a54] leading-relaxed">
              Con la esperanza de que hagamos de este día un recuerdo inolvidable.
            </p>
            <p class="parrafo-animado font-charm text-xl md:text-2xl lg:text-3xl text-[#626a54] leading-relaxed">
              Los esperamos con los corazones llenos de emoción, listos para celebrar el amor y crear memorias que permanezcan siempre en nuestros corazones.
            </p>
          </div>

          <div class="flex items-center justify-center gap-4 my-10" id="linea-decorativa">
            <div class="w-20 h-[1px] bg-gradient-to-r from-transparent via-[#C9A961] to-[#C9A961]"></div>
            <div class="relative">
              <svg class="w-8 h-8 text-[#C9A961]" viewBox="0 0 24 24" fill="currentColor">
                <path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/>
              </svg>
            </div>
            <div class="w-20 h-[1px] bg-gradient-to-l from-transparent via-[#C9A961] to-[#C9A961]"></div>
          </div>

          <div id="seccion-confirmar" class="mt-10">
            <p class="font-charm text-2xl md:text-3xl lg:text-4xl text-[#C9A961] font-bold mb-8" id="texto-confirmar">
              Esperamos contar con tu presencia
            </p>
            <div class="flex justify-center mb-10" id="boton-confirmar">
              <a
                href="https://wa.me/59178762918?text=Me%20complace%20confirmar%20mi%20asistencia%20para%20la%20boda.%20Nos%20vemos%20pronto"
                target="_blank"
                rel="noopener noreferrer"
              >
                <img
                  src="/imagenes/ConfirmarAsistencia.webp"
                  alt="Confirmar asistencia por WhatsApp"
                  class="w-64 md:w-72 lg:w-72 h-auto object-contain cursor-pointer transition-transform duration-300 hover:scale-105 hover:brightness-110"
                />
              </a>
            </div>
          </div>
        </div>
      </div>

      <div class="flex justify-center mt-10" id="flores-abajo-despedida">
        <img
          src="/imagenes/Separadores.webp"
          alt="Flores decorativas"
          class="w-40 md:w-48 lg:w-56 h-auto object-contain opacity-90 "
        />
      </div>
    </div>
  </div>
</section>

<script>
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';

  gsap.registerPlugin(ScrollTrigger);

  if (typeof window !== 'undefined') {
    document.addEventListener('DOMContentLoaded', () => {
      console.log('💑 Iniciando sección de despedida de boda...');

      // 🎯 ESTADO INICIAL - Todos con scale para animación simultánea
      // Agrupamos todos los IDs y clases en una sola variable para más limpieza y facilidad de uso
      const elementosAnimar = '#flores-arriba-despedida, #card-despedida, #titulo-despedida, .parrafo-animado, #linea-decorativa, #texto-confirmar, #boton-confirmar, #flores-abajo-despedida';
      
      gsap.set(elementosAnimar, { opacity: 0, scale: 0.8 });
      
      // ✨ ANIMACIÓN SIMULTÁNEA - MÁXIMA FLUIDEZ
      ScrollTrigger.create({
        trigger: '#flores-arriba-despedida',
        start: 'top 85%',
        
        onEnter: () => {
          const tl = gsap.timeline();

          // Animamos TODOS los elementos JUNTOS.
          // Usamos un pequeño 'stagger' (0.05) para evitar la carga de todos en el mismo instante (lo que causa temblor)
          // pero es tan rápido que parece simultáneo.
          tl.to(elementosAnimar, {
            opacity: 1,
            scale: 1,
            duration: 1.2,
            ease: 'back.out(1.4)',
            stagger: 0.05, // Retraso muy pequeño para simular simultaneidad fluida
          }, 0); // El '0' asegura que todas las animaciones empiecen inmediatamente

          // 🌸 ANIMACIONES CONTINUAS (loopeadas) - Puestas dentro del 'onEnter'
          // Las animaciones loop deben matarse si el usuario hace scroll hacia arriba (onLeaveBack)
          
          // Corazón latiendo
          gsap.to('#linea-decorativa svg', {
            scale: 1.3,
            duration: 1.5,
            ease: 'sine.inOut',
            yoyo: true,
            repeat: -1,
            delay: 1.0, // Un pequeño delay para que empiece después de la animación de entrada
          });

          // Botón pulsando suavemente
          gsap.to('#boton-confirmar img', {
            scale: 1.05,
            y: -5,
            duration: 2.5,
            ease: 'sine.inOut',
            yoyo: true,
            repeat: -1,
            delay: 1.0, 
          });

          // Texto confirmar con brillo sutil
          gsap.to('#texto-confirmar', {
            textShadow: '0 0 15px rgba(201, 169, 97, 0.5)',
            duration: 2.5,
            ease: 'sine.inOut',
            yoyo: true,
            repeat: -1,
            delay: 1.0, 
          });
        },
        
        onLeaveBack: () => {
          // 🔥 Detener las animaciones loopeadas (crucial para evitar consumo de CPU y bugs)
          gsap.killTweensOf('#linea-decorativa svg');
          gsap.killTweensOf('#boton-confirmar img');
          gsap.killTweensOf('#texto-confirmar');
          
          // Resetear todo (usando la variable de agrupación)
          gsap.set(elementosAnimar, { opacity: 0, scale: 0.8 });
        },
      });

      console.log('💑✨ ¡Sección de despedida activada con éxito!');
    });
  }
</script>

<style>
  /* 🛑 ELIMINAMOS EL BLOQUE <style> DUPLICADO y el código de hover CSS se mantiene */
  #card-despedida {
    transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  }

  #card-despedida:hover {
    transform: translateY(-5px);
    box-shadow: 0 20px 40px rgba(201, 169, 97, 0.2);
  }

  /* 🔥 PROPIEDAD CLAVE PARA MEJORAR EL RENDIMIENTO DE SCROLL/ANIMACIÓN */
  /* Esta regla instruye al navegador a optimizar la animación de estas propiedades en la GPU */
  #flores-arriba-despedida,
  #card-despedida,
  #titulo-despedida,
  .parrafo-animado,
  #linea-decorativa,
  #texto-confirmar,
  #boton-confirmar,
  #flores-abajo-despedida {
    will-change: transform, opacity; 
  }
</style>